# brikdesigns/homebrew-tap

Homebrew tap for Brik's internal macOS tools.

```bash
brew tap brikdesigns/tap
brew install --cask quickactions
```

Casks are version-bumped automatically by each tool's release workflow.
